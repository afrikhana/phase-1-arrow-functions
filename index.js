const divide = (param1, param2) => {
    console.log(`Adding ${param1}`);
    console.log(`Adding ${param2}`);
    return param1 / param2;
  }
  divide(2000,100);


const square = (x) => x*x;
square(2);

const add= (x,y) => x+y;
add(3,4);